require "spec_helper"

describe AptFinder do
  it "has a version number" do
    expect(AptFinder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
